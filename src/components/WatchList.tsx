import React from 'react';
import Header from "./Header";

const WatchList = () => {
    return (
        <div>
            <Header filter={false}/>
        </div>
    );
};

export default WatchList;